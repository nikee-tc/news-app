import React from "react";
import {render} from '@testing-library/react';
import Card from "./Card";

describe('Card component', () =>{
     test('render the card with provided props' , () => {

     })
});